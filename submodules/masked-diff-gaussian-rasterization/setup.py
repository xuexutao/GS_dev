from setuptools import setup, find_packages


setup(
    name="masked-diff-gaussian-rasterization",
    version="0.0.1",
    description="Mask-aware wrapper around diff-gaussian-rasterization",
    packages=find_packages(),
    install_requires=[],
)

